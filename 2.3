queries = [
    'смотреть сериалы онлайн',
    'новости спорта',
    'афиша кино',
    'курс доллара',
    'сериалы этим летом',
    'курс по питону',
    'сериалы про спорт',
]
data = []
for i in queries:
    data.append(i.split())
print(data)
j = []
for x in data:
    if len(x) == 2:
        j = 100//len(queries)//2
    elif len(x) == 3:
        j = 100//len(queries)//3
    elif len(x) == 1:
        j = 100//len(queries)//1
    print(j)
